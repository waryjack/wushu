<table>
{{#each specials as |item|}}
    <tr>
        <td><a data-action="editItem" data-item-id="{{item.id}}">{{item.name}}</a></td>
        <td><a data-action="deleteItem" data-item-id="{{trait.id}}"><i class="fa-solid fa-trash-can"></i></a></td>
    </tr>
{{/each}}
</table>