<div>
    <h3>{{localize "WUSHU.item.features"}}</h3>
    <prose-mirror name="system.features" 
                      data-document-u-u-i-d="{{item.uuid}}" 
                      value="{{enrichedDescription}}" 
                      collaborate="true" 
                      toggled="false">
            {{{enrichedDescription}}}
    </prose-mirror>
</div>